# Vision Validation Module - Optional Dependencies
#
# Install all dependencies:
#   pip install -r requirements.txt
#
# Or install specific feature sets:
#   pip install -r requirements.txt --no-deps  # then install what you need

# Core dependencies (required)
numpy>=1.20.0

# Image handling (highly recommended)
Pillow>=9.0.0

# Computer vision (recommended for most features)
opencv-python>=4.5.0
# OR for more features (larger download):
# opencv-contrib-python>=4.5.0

# Fast screenshot capture (optional, improves performance)
mss>=6.1.0

# OCR - Option 1: Tesseract (lightweight, requires system install)
pytesseract>=0.3.8
# System requirement: Install Tesseract OCR
#   Ubuntu/Debian: sudo apt-get install tesseract-ocr
#   macOS: brew install tesseract
#   Windows: Download from https://github.com/UB-Mannheim/tesseract/wiki

# OCR - Option 2: EasyOCR (heavier but no system deps, better accuracy)
# easyocr>=1.6.0
# Note: EasyOCR is ~500MB download, includes deep learning models

# AI Vision (optional, for advanced features)
# transformers>=4.20.0  # For CLIP, DETR models
# torch>=1.12.0  # Required for transformers
# ultralytics>=8.0.0  # For YOLO models

# Image processing utilities (optional enhancements)
# scikit-image>=0.19.0  # Additional image processing
# imagehash>=4.3.0  # Perceptual hashing
